version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 24
        - node --version
        - node --version
        - npm --version
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
  cache:
    paths:
      - .npm/**/*
